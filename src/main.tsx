/**
 * It is not necessary to implement bootstrap as it's handled by vite.
 * But this import ensure it runs asynchronously (notice it is a function and not a synch import "./bootstrap") and made portable for other frameworks.
 */

import("./bootstrap");
